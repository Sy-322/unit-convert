var math = (function (mathjs) {
  'use strict';

  // build.js

  // 使用する機能だけ指定（例：evaluate, bignumber, config, format）
  const config = {};
  const customMath = mathjs.create(
    {
      config: true,
      bignumber: true,
      evaluate: true,
      format: true,
      divide: true,
      multiply: true,
    },
    config
  );

  return customMath;

})(mathjs);
